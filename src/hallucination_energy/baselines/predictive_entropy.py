"""Token-level predictive entropy / sequence log-likelihood baseline (planned).

TODO: compute from ``token_log_likelihoods`` already captured during
generation (see ``generation.hf_model``) — this is the simplest baseline in
the plan's comparison table and requires no additional sampling.
"""
