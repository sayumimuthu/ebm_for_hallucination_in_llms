"""Ground-truth accuracy metrics (``accuracy.py``), detection metrics
(``metrics.py``: AUROC/AUPRC/ECE), the N x 4 energy diagnostic
(``diagnostics.py``), and risk-coverage evaluation (``selective_prediction.py``:
AURAC). ``efficiency.py`` (cost-aware adaptive inference accounting) is
still planned — see that module's docstring.
"""
